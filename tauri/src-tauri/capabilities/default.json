{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the PDF Signer window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
